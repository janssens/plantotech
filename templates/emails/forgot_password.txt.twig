{# templates/emails/forgot_password.txt.twig #}

Bonjour {{ user.username }},
Pour définir un nouveau mot de passe visite {{ url('app_reset_password',{'token':user.rpToken}) }} avant le {{ user.rpDelay | date("d M Y à H:i") }}
A très bientôt