<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% block head %}{% endblock %}
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ base_url }}css/knacss/css/knacss.css"/>
        <link rel="stylesheet" href="{{ base_url }}css/style.css"/>
        <link rel="stylesheet" href="{{ base_url }}lib/dropzone/min/dropzone.min.css"/>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <div class="grid">
            <div>
                <a href="{{ base_url }}">Accueil</a> / <a href="{{ path('plant_index') }}">🌿 les plantes</a>
            </div>
            <div class="txtright">
                {% if is_granted('ROLE_USER') %}
                   <b> {% if is_granted('ROLE_ADMIN') %}🌳{% else %}🧘{% endif %} {{ app.user.username }} </b> |  🔓<a href="{{ path('app_logout') }}">déconnection</a>
                {% else %}
                    <a href="{{ path('app_login') }}">connexion</a> | <a href="{{ path('app_register') }}">s'inscrire</a>
                {% endif %}
            </div>
        </div>
        <div class="grid-12-small-4">
            {% for type,messages in app.session.flashBag.all %}
                {% if type == 'error' %}{% set type = 'danger' %}{% endif %}
                {% for message in messages %}
                    <div class="col-4"></div>
                    <div class="col-4 alert--{{ type }} txtcenter">{{ message }}</div>
                    <div class="col-4"></div>
                {% endfor %}
            {% endfor %}
        </div>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        <div id="footer">
            <small>Un bug 🐛 ? Une suggestion 💡 ? Utilise <a href="{{ path('contact') }}">ce formulaire</a> !</small>
        </div>
        <script src="{{ base_url }}js/jquery-3.5.1.min.js"></script>
        <script src="{{ base_url }}js/app.js"></script>
        <script src="{{ base_url }}lib/dropzone/min/dropzone.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
