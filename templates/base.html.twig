<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% block head %}{% endblock %}
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="/css/knacss/css/knacss.css"/>
        <link rel="stylesheet" href="/css/style.css"/>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <div class="grid">
            <div>
                <a href="/">🏠 Accueil</a> > <a href="{{ path('plant_index') }}">🌿 plantes</a>
            </div>
            <div>
                {% if is_granted('ROLE_USER') %}
                   <b> 🧘 {{ app.user.username }} </b> |  🔓<a href="{{ path('app_logout') }}">déconnection</a>
                {% else %}
                    <a href="{{ path('app_login') }}">connexion</a> | <a href="{{ path('app_register') }}">s'inscrire</a>
                {% endif %}
            </div>
        </div>

        {% block body %}{% endblock %}
        <script src="/js/jquery-3.5.1.min.js"></script>
        <script src="/js/app.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
