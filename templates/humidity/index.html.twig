{% extends 'base.html.twig' %}

{% block title %}Humidity index{% endblock %}

{% block body %}
    <h1>Humidity index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Value</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for humidity in humidities %}
            <tr>
                <td>{{ humidity.id }}</td>
                <td>{{ humidity.value }}</td>
                <td>
                    <a href="{{ path('humidity_show', {'id': humidity.id}) }}">show</a>
                    <a href="{{ path('humidity_edit', {'id': humidity.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('humidity_new') }}">Create new</a>
{% endblock %}
