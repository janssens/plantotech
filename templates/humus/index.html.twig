{% extends 'base.html.twig' %}

{% block title %}Humus index{% endblock %}

{% block body %}
    <h1>Humus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for humu in humuses %}
            <tr>
                <td>{{ humu.id }}</td>
                <td>{{ humu.quantity }}</td>
                <td>
                    <a href="{{ path('humus_show', {'id': humu.id}) }}">show</a>
                    <a href="{{ path('humus_edit', {'id': humu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('humus_new') }}">Create new</a>
{% endblock %}
