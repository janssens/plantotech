{% extends 'base.html.twig' %}

{% block title %}Humus{% endblock %}

{% block body %}
    <h1>Humus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ humu.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ humu.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('humus_index') }}">back to list</a>

    <a href="{{ path('humus_edit', {'id': humu.id}) }}">edit</a>

    {{ include('humus/_delete_form.html.twig') }}
{% endblock %}
