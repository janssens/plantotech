{% if plant.images | length %}
    <div class="flex flex-wrap justify-start">
        {% for image in plant.images %}
            {% if (loop.index != 1) or (hide_first != 1) %}
                <a href="{{ asset(image.src) }}" data-fslightbox class="block flex-none"><img width="150px" class="border-4 border-white shadow-lg m-2" src="{{ asset(image.src)|imagine_filter('square') }}" alt=""></a>
            {% endif %}
        {% endfor %}
    </div>
{% else %}
    <span style="color:orange;">Aucune image associée</span>
{% endif %}