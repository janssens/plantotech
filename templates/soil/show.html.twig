{% extends 'base.html.twig' %}

{% block title %}Soil{% endblock %}

{% block body %}
    <h1>Soil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soil.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ soil.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('soil_index') }}">back to list</a>

    <a href="{{ path('soil_edit', {'id': soil.id}) }}">edit</a>

    {{ include('soil/_delete_form.html.twig') }}
{% endblock %}
