{% extends 'base.html.twig' %}

{% block title %}Plantotech{% endblock %}

{% block body %}
    <h1 class="content-center text-center text-3xl p-5">
        Plantothèque des Alvéoles
    </h1>

    <div class="text-center">
        <img class="max-w-sm m-auto" src="{{ asset('build/img/home.svg') }}" alt="Plantothèque">
    </div>

    <div class="m-auto max-w-full w-1/2">
        Bonjour {% if is_granted('ROLE_USER') %}{{ app.user.username }} 👋{% endif %} !

        <p>
            Cette base de donnée est le fruit d’un travail collaboratif commencé en 2018, coordonnée et dirigé par <a href="https://www.atelier-alveoles.fr/">l’Atelier des Alvéoles</a>.
            <br>
            Nous avons chercher à rassembler toutes les informations utiles pour concevoir et cultiver un jardin-foret, selon le design de permaculture.
            <br>
            Le vivant ne se laisse pas facilement rangé dans des cases... aussi, malgré tous nos efforts, certains critères peuvent être sujet à discussion.
            {% if is_granted('ROLE_USER') %}<br>
            Nous vous laissons le soin de confronter ces informations à d’autres sources. Ce travail évolue avec vous, n’hésites pas à nous faire part de vos suggestions,
            ajout, coquilles relevées,... Vous pouvez pour cela utiliser <a href="{{ path('contact') }}">ce formulaire</a>.{% endif %}
        </p>
    </div>

    <div class="m-2">
        {% if is_granted('ROLE_USER') %}
            <div class="text-center">
                <button type="button" data-href="{{ path('plant_index') }}" class="btn btn--dark">Entrer dans la jungle 🌳 !</button>
            </div>
        {% else %}
            <div class="text-center pb-5">
                <p>Pour utiliser cet outil, il faut se connecter 🤝 </p>
                <button type="button" data-href="{{ path('app_register') }}" class="m-auto focus:outline-none block mt-5 text-white text-sm py-2.5 px-5 border-b-4 border-blue-600 rounded-md bg-blue-500 hover:bg-blue-400">je rejoins la tribu 🏕️</button>
                <small>Déjà un compte ? je me <a href="{{ path('app_login') }}" class="underline">connecter ici</a>🔒</small>
            </div>
        {% endif %}
    </div>

{% endblock %}
